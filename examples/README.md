# Examples

Place demo `.txt` files here, such as:

- `chun.txt`
- `guxiang.txt`

Recommended validation queries:

- `春天` should return `春`
- `花草` should return `春`
- `少年闰土` should return `故乡`
- `小孩子` should return `故乡`
- `乡下少年` should return `故乡`

